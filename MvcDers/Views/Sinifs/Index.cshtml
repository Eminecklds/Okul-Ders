@model IEnumerable<MvcDers.Models.Sinif>

@{
    ViewBag.Title = "Index";
}

<h2>@Html.ActionLink("Anasayfa", "Index", "Home")</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SinifAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SinifAdi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SinifID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SinifID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SinifID })
        </td>
    </tr>
}

</table>
