@model IEnumerable<MvcDers.Models.Ders>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DersAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DersAdi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DersID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DersID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DersID })
        </td>
    </tr>
}

</table>
